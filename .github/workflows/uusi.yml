name: Uusi
on: push
jobs:
  testi:
    if: ${{github.event.head_commit.message == 'yes'}}
    env:
      TEMP: ${{toJson(github.event.head_commit)}}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Filu on $TEMP"
      

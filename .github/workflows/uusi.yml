name: Uusi
on: push
jobs:
  testi:
    if: ${{github.event.head_commit.message == 'yes'}}
    env:
      TEMP: ${{toJson(github.event.commits)}}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Filu on $TEMP"
      
